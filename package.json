{
  "name": "typescript-unit-test",
  "version": "1.0.0",
  "description": "Unit testing framework for typescript",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "compile": "tslint -c tslint.json 'src/**/*.ts' && tsc",
    "example": "node bin --project example/tsconfig.json example/**/*.spec.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anthony Cleaver",
  "license": "ISC",
  "dependencies": {
    "glob": "^7.1.1",
    "ts-node": "^1.7.2"
  },
  "devDependencies": {
    "tslint": "^4.0.2",
    "typescript": "^2.1.4"
  }
}
